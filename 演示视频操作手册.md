# 🎬 PetNode 演示视频操作手册

> 目标：冲刺优秀（18-20 分），时长 12-14 分钟
> 服务器：47.108.239.80 | 域名：pppetnode.com

---

## ⚠️ 录屏前必做（30 分钟前完成）

### 1. 服务器预热

打开第一个终端窗口，SSH 到服务器：

```bash
ssh root@47.108.239.80
# 密码: 1145141919810#Homo
```

依次执行以下命令确认一切正常：

```bash
cd /opt/petnode/C_end_Simulator

# ① 确认 6 个容器全部 Up
docker compose ps

# ② 确认 Engine 在产数据
docker compose logs --tail=5 engine

# ③ 确认 MQ Worker 在消费（MySQL 报错可忽略，已弃用）
docker compose logs --tail=3 mq-worker

# ④ 确认健康检查
curl -s http://localhost:5000/api/health
# 预期: {"status":"healthy","total_received":0,...}
# 注意：total_received=0 是因为数据走 MQ 通道而非 HTTP 通道，这是正确的

# ⑤ 确认 MongoDB 有数据
docker exec petnode-mongodb mongosh --quiet petnode --eval 'db.received_records.countDocuments({})'
# 预期：> 50000
```

### 2. 本地工具准备

打开以下窗口并保持就绪：

| # | 工具 | 用途 | 录屏方式 |
|---|------|------|---------|
| ① | **终端 1** | SSH 到服务器，展示 Docker / 日志 | 窗口录屏 |
| ② | **终端 2** | 本地跑 demo_classroom.py | 窗口录屏 |
| ③ | **Postman** | API 鉴权演示（可选，可被 demo 脚本替代） | 窗口录屏 |
| ④ | **微信开发者工具** | 小程序演示 | 窗口录屏 |
| ⑤ | **浏览器** | Web 首页 + Admin 大屏 | 窗口录屏 |
| ⑥ | **VS Code** | 展示关键代码片段 | 窗口录屏 |

### 3. 小程序准备

```
打开微信开发者工具 → 导入项目 → 选择 d:\bot\PetNode\wechat\WeChat_miniprogram
AppID 选"测试号"或"不使用云服务"
确认 utils/api.js 中 BASE_URL = 'https://pppetnode.com'
```

### 4. 浏览器准备

提前打开两个标签页但先不展示：
- 标签页 1：`https://pppetnode.com` （首页）
- 标签页 2：`https://pppetnode.com/admin.html` （管理大屏）

### 5. 演示脚本准备

```bash
cd d:\bot\PetNode\C_end_Simulator
# 确认 Python 版本 ≥ 3.10（已修复 3.9 兼容）
python --version
```

### 6. OBS 录屏设置

- 录制模式：全屏或区域
- 音频：系统音频 + 麦克风
- 分辨率：1920×1080
- 帧率：30fps
- 提前试录 10 秒确认音画同步

---

## 🎥 正式录制——分步操作

---

### 【开场】0:00 – 0:40 — 项目概述

**操作**：展示 PPT 或直接展示 VS Code 项目根目录

**步骤**：
1. 打开 VS Code，展开 PetNode 项目目录树
2. 展示 README.md 中的架构图

**讲解词**：
> PetNode 是一套智能宠物项圈监控系统。我们小组实现了一个完整的物联网数据管道——从设备数据模拟，到消息传输、鉴权存储，再到微信小程序和 Web 大屏的多端消费。下面我按数据流动顺序来演示。

> 小组分工：我负责后端架构和模拟引擎，XXX 负责微信小程序，XXX 负责 Web 前端。

---

### 【第一段】架构总览 — 0:40 – 2:30

#### 步骤 1.1：展示架构图（0:40 – 1:10）

**操作**：在 VS Code 中打开 `README.md`，滚到架构图部分

**讲解词**：
> 系统分为三层。C 端——数据引擎集群模拟 10 条智能项圈；S 端——Docker 微服务集群负责接收、鉴权、分发、存储；前端——覆盖微信小程序、Web 管理大屏、终端 TUI 三种消费端。

> 技术架构上，我们使用了 Docker Compose 编排 6 个微服务：RabbitMQ 消息队列、MongoDB 文档数据库、Flask API 服务器、MQ Worker 消费者、Engine 数据引擎。服务间通过健康检查保证启动顺序，MQ 负责引擎和存储层解耦。

#### 步骤 1.2：SSH 登录展示容器状态（1:10 – 2:00）

**操作**：切换到终端 1，执行：

```bash
ssh root@47.108.239.80
cd /opt/petnode/C_end_Simulator
docker compose ps
```

**讲解词**：
> 这是阿里云上的生产服务器，Debian 13 系统。可以看到 6 个容器全部在 healthy 状态。Flask 映射 5000 端口对外提供 API，MongoDB、MySQL、RabbitMQ 提供数据基础设施，Engine 在持续产生数据——已经跑了 9 小时。

> 这套系统一行命令 `docker compose up -d` 就能完成部署。通过 Nginx 反向代理 + Let's Encrypt 自动 SSL 证书，用户通过 pppetnode.com 域名即可访问。

#### 步骤 1.3：展示 docker-compose.yml 关键配置（2:00 – 2:30）

**操作**：在 VS Code 中打开 `C_end_Simulator/docker-compose.yml`

滚动展示关键段落：
- `depends_on` 健康检查依赖（第 126-194 行）
- Engine 的 `EXPORT_BACKEND=mq` 环境变量（第 183 行）
- 共享卷 `output_data`（第 175 行）

**讲解词**：
> 注意这里 Engine 的启动命令——10 条狗、1 秒间隔、固定种子 99，保证每次部署设备 ID 一致。EXPORT_BACKEND 环境变量设为 mq，意味着数据通过 RabbitMQ 异步推送，而非 HTTP 直推。这种设计的优势是：即使 Flask 挂了，消息也不会丢失——RabbitMQ 持久化队列会自动缓存。

---

### 【第二段】模拟引擎 + 数据流 — 2:30 – 5:00

#### 步骤 2.1：实时引擎日志（2:30 – 3:15）

**操作**：在终端 1 中执行：

```bash
docker compose logs --tail 20 -f engine
# 保持滚动 15-20 秒，然后 Ctrl+C 退出
```

**讲解词**：
> 这是 Engine 的实时日志。每 20 条（即 20 秒）会打印一次汇总：进度到多少 tick、MQ 发布多少条。当前已跑到 3 万多 tick，发布了 31 万多条记录。每秒 10 条 × 每条 12 字段 = 每秒处理 120 个数据点。

> 引擎的核心是**马尔可夫链行为模型**。不是简单的随机行为——而是把一天分成四个时段（深夜、上午、白天、傍晚），每个时段有不同的行为转移概率矩阵。比如深夜 70% 概率维持睡觉，而白天有 40% 概率进入散步。这样才能模拟出合理的、可分析的数据。

#### 步骤 2.2：MQ Worker 消费日志（3:15 – 3:40）

**操作**：终端 1 中执行：

```bash
docker compose logs --tail 15 -f mq-worker
# 展示 5-10 秒后 Ctrl+C
```

**讲解词**：
> 这是 MQ Worker 的消费日志。每条记录从 RabbitMQ 取出后，先做 **API Key + HMAC-SHA256 签名验证**，验证通过后写入 MongoDB。注意现在 MySQL 因为已弃用所以报错，但 MongoDB 写入是成功的——这就是**降级容错**的设计：非关键组件失败不影响核心写入路径。

#### 步骤 2.3：MongoDB 数据验证（3:40 – 4:20）

**操作**：终端 1 中执行以下命令，逐条展示：

```bash
# ① 数据总量
docker exec petnode-mongodb mongosh --quiet petnode --eval 'print("总记录数: " + db.received_records.countDocuments({}))'
# 预期：>50000

# ② 设备分布
docker exec petnode-mongodb mongosh --quiet petnode --eval 'db.received_records.distinct("device_id").forEach(d => print("  " + d + ": " + db.received_records.countDocuments({device_id: d}) + " 条"))'

# ③ 最新 2 条完整数据
docker exec petnode-mongodb mongosh --quiet petnode --eval 'db.received_records.find().sort({_id:-1}).limit(2).forEach(r => print(JSON.stringify(r, null, 2)))'
```

**讲解词**：
> 验证一下数据质量。当前 5.4 万条记录，10 台设备均匀分布各约 5400 条。来看一条完整数据——包含 12 个字段：device_id、时间戳、行为状态、心率、呼吸、体温、步数、电量、GPS 经纬度、事件类型、事件阶段。一台设备从 sleeping 变为 walking，心率从 ~60 升到 ~115——符合真实规律。

> 其中有 1.2 万条包含健康事件，如发烧和受伤，每个事件遵循 onset→peak→recovery 完整生命周期。

#### 步骤 2.4：API 健康检查验证（4:20 – 4:40）

**操作**：终端 1 中执行：

```bash
curl -s http://localhost:5000/api/health | python3 -m json.tool
curl -s https://pppetnode.com/api/health | python3 -m json.tool
```

**讲解词**：
> 健康检查本地返回正常。通过域名 https://pppetnode.com/api/health 同样正常——这是 Nginx 反向代理在起作用，把 /api/ 路径转发到内部 Flask 5000 端口，SSL 由 Let's Encrypt 提供。

#### 步骤 2.5：Admin API 数据验证（4:40 – 5:00）

**操作**：终端 1 中执行：

```bash
curl -s http://localhost:5000/api/v1/admin/stats | python3 -m json.tool
```

**讲解词**：
> Admin 统计接口返回了丰富的聚合数据：10 台在线设备、40 个活跃事件、心率分布（正常 172 条、心动过速 10 条、偏低 18 条）、行为分布及对应的平均心率。在 running 状态下平均心率 147，sleeping 下只有 67——验证了引擎的行为-生理关联模型。

---

### 【第三段】接口鉴权演示 — 5:00 – 7:00

#### 步骤 3.1：运行 demo_classroom.py（5:00 – 6:30）

**操作**：切换到终端 2（本地），执行：

```bash
cd d:\bot\PetNode\C_end_Simulator
python scripts/demo_classroom.py --host 47.108.239.80 --api-key 0568bc32e78ef81746be44e96c3a66d1fd18867b11752717bdb460d58f15f827 --hmac-key 471a233d74427fc24927ea71d067a2e0b08f90cbbe2446f14b243692ba18c045 --batch 20
```

脚本自动按顺序执行并输出彩色结果，跟着讲解即可：

**讲解词**：
> 演示脚本按四个阶段排列。第一阶段连通性验证——GET /api/health 返回 200。

> 现在是核心部分——**双层鉴权机制**。Engine 上传数据需要同时提供 API Key 和 HMAC-SHA256 签名。我演示四种攻击场景：
> - 无 Authorization 头 → 401 Unauthorized
> - 错误的 API Key → 401  
> - 有 API Key 但缺 HMAC 签名 → 403 Forbidden
> - **篡改检测**：用原始数据的签名 + 篡改后的 body，服务端验签失败 → 403
> - 正确 API Key + 正确签名 → 200 OK，数据上传成功

> HMAC 的关键在于：服务端先接收 body 字节流，用共享密钥重新计算签名，再用 `hmac.compare_digest()` 做常量时间比较——防止了时序攻击。任何中间代理修改了 body，签名就不匹配，数据被拒绝。这是我们在安全方面的重点设计。

> 第三阶段批量上传 20 条模拟数据，每条独立签名，可以看到逐条的成功/失败和耗时。最后健康检查验证 total_received 增量，确认数据确实写入。

#### 步骤 3.2：Postman 补充演示（可选）（6:30 – 7:00）

**操作**：切换到 Postman

1. 新建请求：POST `http://47.108.239.80:5000/api/data`
2. Headers：
   - `Content-Type: application/json`
   - `Authorization: Bearer <API_KEY>`
   - `X-Signature: <HMAC签名>`
3. Body (raw JSON)：粘贴一条记录
4. 发送，展示 200 响应

**讲解词**：
> 在 Postman 中也可以直观看到：三个请求头缺一不可，签名必须与 body 完全匹配。这就是我们在数据接口层的安全保障。

---

### 【第四段】Web 端 — 7:00 – 9:00

#### 步骤 4.1：官网首页（7:00 – 7:20）

**操作**：切换到浏览器标签页 1，访问 `https://pppetnode.com`

滚动展示页面内容

**讲解词**：
> 这是 Web 端落地页，展示产品功能和品牌形象。通过 Nginx 部署在 pppetnode.com，SSL 自动续期保障安全。

#### 步骤 4.2：管理大屏（7:20 – 9:00）

**操作**：切换到浏览器标签页 2，访问 `https://pppetnode.com/admin.html`

按以下顺序展示：

1. **顶部**——实时时间、LIVE 指示灯（脉冲动画）
2. **左侧**——10 台设备卡片，（behavior + heart_rate），可以滚动
3. **中央**——心率分布饼图 / 行为分布柱状图
4. **右侧**——事件流（有 event 的设备高亮）

**操作**：打开浏览器开发者工具（F12）→ Network 标签，刷新页面，展示 API 请求

**讲解词**：
> 这是管理大屏。暗色主题专业监控风格，LIVE 指示灯脉冲动画表示实时数据刷新。

> 左侧是 10 台在线设备的实时快照——每个卡片显示设备 ID、行为状态、心率、呼吸、GPS 坐标。红色标注的设备表示有活跃健康事件。

> 中间是数据可视化区域：心率分布饼图——大部分在 60-140 正常区间，少量低于 60（睡眠状态），个位数超过 140（运动或发烧）。行为分布——睡觉、休息、散步、奔跑的比例符合狗狗真实作息。

> 打开 F12 看 Network：页面调用 `/api/v1/admin/stats` 和 `/api/v1/admin/devices/realtime` 两个接口，每秒轮询一次。接口返回近 200 条采样数据的聚合统计。

> 点击某台设备可以展开历史曲线——心率、呼吸、体温的时序变化，数据源是后端实时生成的模拟数据。

---

### 【第五段】微信小程序 — 9:00 – 11:30

#### 步骤 5.1：登录流程（9:00 – 9:40）

**操作**：切换到微信开发者工具

1. 点击编译 → 模拟器中显示小程序
2. 进入登录页 → 勾选用户协议 → 点击"一键登录"

**讲解词**：
> 这就是微信小程序端，共 13 个页面。先看登录流程——用户点击一键登录后：
> 1. `wx.login()` 获取临时 code
> 2. POST `/api/v1/wechat/auth` 将 code 发给服务端
> 3. 服务端调用微信 code2Session（开发阶段 mock 模式）
> 4. 返回 `wx_identity_token`（10 分钟有效期）
> 5. 自动调用 `/api/v1/wechat/bind` 完成用户创建和绑定
> 6. 返回 JWT `access_token`（7 天有效期），存入本地 Storage

> 后续所有 API 请求都由 `utils/api.js` 统一注入 Bearer token，过期自动跳转登录页。

#### 步骤 5.2：首页 + 设备绑定（9:40 – 10:10）

**操作**：
1. 登录后展示宠物列表页
2. 展示扫码绑定功能（演示界面即可）

**讲解词**：
> 登录后进入首页。此时宠物列表有一个已绑定设备"大黄"。支持下拉刷新、滑动切换页卡。

> 扫码绑定功能——扫描二维码调用 `wx.scanCode` 获取设备 ID，调用 `/api/v1/devices/bind` 完成认领。

#### 步骤 5.3：宠物详情页（10:10 – 11:00）

**操作**：点击大黄 → 进入宠物详情页

1. 展示心率时序折线图
2. 切换到呼吸/体温曲线
3. 展示 GPS 地图定位
4. 如果设备有事件，展示事件红点

**讲解词**：
> 这是宠物详情页。心率、呼吸、体温三条时序曲线，数据每 1 秒一条，通过 `/api/v1/pets/{pet_id}/heart-rate/series` 等接口查询。每个数据点来自 Engine 的实时模拟——比如 running 状态时心率飙到 130，sleeping 时降到 58。

> GPS 定位展示在地图上，通过 `/api/v1/pets/{pet_id}/location/latest` 获取最新经纬度。当狗狗在不同行为状态时，位置变化幅度不同——走路/跑步时位移大，睡觉时几乎不动。

#### 步骤 5.4：家庭组功能（11:00 – 11:30）

**操作**：
1. 回到首页 → 点击邀请家人按钮
2. 展示生成邀请链接流程
3. 切换到家庭管理页 `familyManage`

**讲解词**：
> 家庭组是我们的特色功能。用户可以创建家庭、生成邀请链接、其他家庭成员扫码加入。加入后能共享查看所有绑定宠物的数据。

> 技术实现上，涉及 5 个 MongoDB 集合的联合操作：families（家庭组）、family_members（成员关系）、family_invites（邀请码）、user_pets（绑定关系）、received_records（遥测数据）。邀请码有过期机制，成员有 owner/member 角色区分。

---

### 【第六段】技术亮点 + 总结 — 11:30 – 13:00

#### 步骤 6.1：技术亮点回顾（11:30 – 12:30）

**操作**：切回 VS Code，展示关键代码文件

展示以下文件的关键部分：

1. `engine/models/smart_collar.py`——行为马尔可夫链的转移矩阵代码
2. `flask_server/app.py`——HMAC 验签逻辑（`_calc_sig` + `compare_digest`）
3. `flask_server/auth.py`——JWT 生成和验证
4. `engine/exporters/mq.py`——MQ 导出器
5. `flask_server/mq_worker.py`——MQ 消费者验签流程

**讲解词**：
> 最后总结几个关键技术亮点：

> **① 行为马尔可夫链**——不是随机，而是基于时段×当前状态的条件概率转移。每个时段有独立的 4×4 转移矩阵，使模拟数据具备真实的分析价值。

> **② 双通道数据管道**——Engine 支持 HTTP 和 MQ 两种导出模式，通过 `EXPORT_BACKEND` 环境变量一键切换。MQ 模式下 RabbitMQ 提供消息持久化和自动重投递，保证数据不丢失。

> **③ 安全鉴权**——Engine 端用 API Key + HMAC-SHA256 防冒充和防篡改；微信端用 JWT 用户鉴权。HMAC 签名用 `compare_digest` 防时序攻击。

> **④ 降级容错**——MongoDB 是主存储，MySQL 出错不阻塞写入路径。就像刚才看到的 MQ Worker 日志：MySQL 报错但 MongoDB 数据正常写入。

> **⑤ 完整测试覆盖**——系统有 pytest 测试套件，单元测试覆盖数据生成、HMAC 签名、文件导出；集成测试覆盖微信 API 全链路。使用 mongomock 实现本地无 Docker 测试。

#### 步骤 6.2：部署总结（12:30 – 12:50）

**操作**：切回终端 1，展示

```bash
docker compose ps
docker stats --no-stream
```

**讲解词**：
> 部署在阿里云 2C4G 的 Debian 13 服务器上。全栈 Docker 化：6 个容器占用约 1.5G 内存、引擎 CPU 不到 1%。持续运行 9 小时无故障。

> 运维方面有完整手册：SSH 管理、Docker 运维、Nginx + SSL、数据库备份恢复、Jenkins CI/CD 流水线。

#### 步骤 6.3：收尾（12:50 – 13:00）

**讲解词**：
> PetNode 从数据产生到多端消费，覆盖了物联网系统的完整链路——设备模拟 → 消息分发 → 安全鉴权 → 双库存储 → 多端查询 → 健康分析。我们小组通过这个项目，掌握了微服务架构、消息队列、容器化部署、前后端联调等核心技能。谢谢！

---

## 📋 录屏后检查

| 检查项 | ✓ |
|--------|---|
| 时长在 12-14 分钟以内 | ☐ |
| 音频清晰、无杂音 | ☐ |
| 终端字体够大、颜色清晰 | ☐ |
| 每一步操作成功、无卡顿 | ☐ |
| 覆盖了 4 端：引擎 + API + Web + 小程序 | ☐ |
| 提到了关键术语：马尔可夫链、HMAC-SHA256、JWT、RabbitMQ、降级容错 | ☐ |
| 提到了小组分工 | ☐ |
| 视频文件格式正确、可播放 | ☐ |

---

## 🔑 参考：服务器凭证速查

| 项目 | 值 |
|------|-----|
| SSH | `ssh root@47.108.239.80`，密码 `1145141919810#Homo` |
| API 域名 | `https://pppetnode.com` |
| Flask 端口 | `http://47.108.239.80:5000` |
| API Key | `0568bc32e78ef81746be44e96c3a66d1fd18867b11752717bdb460d58f15f827` |
| HMAC Key | `471a233d74427fc24927ea71d067a2e0b08f90cbbe2446f14b243692ba18c045` |
| JWT Secret | `973d33a209720f552c27b7fc2d7e973aae8c9b1f19a99bd7fb222a44b22bd5cb` |
| Admin 登录 | 用户名 `admin`，密码 `d6ead38ffcfd4c83` |
| 演示设备 ID | `105d3d1072e4`（已绑定 pet_name: "大黄"） |

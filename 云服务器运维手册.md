# PetNode 云服务器运维手册

> 服务器 IP：`47.108.239.80` · 域名：`pppetnode.com`  
> 操作系统：Debian 13 · Docker 26 · Nginx 1.26  
> 更新日期：2026-06-11

---

## 目录

1. [SSH 连接](#1-ssh-连接)
2. [目录结构](#2-目录结构)
3. [Docker 服务管理](#3-docker-服务管理)
4. [Nginx 管理](#4-nginx-管理)
5. [SSL 证书](#5-ssl-证书)
6. [日志查看](#6-日志查看)
7. [数据库操作](#7-数据库操作)
8. [备份与恢复](#8-备份与恢复)
9. [更新部署](#9-更新部署)
10. [系统监控](#10-系统监控)
11. [常见问题排查](#11-常见问题排查)
12. [密钥一览](#12-密钥一览)

---

## 1. SSH 连接

```bash
ssh root@47.108.239.80
```

密码：`1145141919810#Homo`（首次连接后建议修改）

> **安全建议**：生产环境建议改用 SSH 密钥认证，禁用密码登录。

---

## 2. 目录结构

```
/opt/petnode/                      ← 后端项目根目录
  └── C_end_Simulator/              ← Docker Compose + 代码
        ├── docker-compose.yml      ← 服务编排文件
        ├── .env                    ← 环境变量（密钥）
        ├── flask_server/           ← Flask API 后端
        ├── engine/                 ← 数据模拟引擎
        └── ui_tui/                 ← 终端监控 UI

/var/www/pppetnode.com/             ← 前端静态文件
  ├── index.html                    ← 官网首页
  ├── admin.html                    ← 管理大屏
  ├── css/
  ├── js/
  └── assets/

/etc/nginx/conf.d/
  └── pppetnode.com.conf            ← Nginx 站点配置
```

---

## 3. Docker 服务管理

所有命令需在 `/opt/petnode/C_end_Simulator/` 目录下执行：

```bash
cd /opt/petnode/C_end_Simulator
```

### 3.1 查看服务状态

```bash
# 所有容器状态
docker compose ps

# 示例输出:
# NAME                STATUS
# petnode-engine      Up 2 hours
# petnode-flask       Up 2 hours (healthy)
# petnode-mongodb     Up 2 hours (healthy)
# petnode-mq-worker   Up 2 hours
# petnode-mysql       Up 2 hours (healthy)
# petnode-rabbitmq    Up 2 hours (healthy)
```

### 3.2 启动/停止/重启

```bash
# 启动所有服务
docker compose up -d

# 停止所有服务（不删除数据卷）
docker compose down

# 停止所有服务并删除数据卷（数据会丢失！）
docker compose down -v

# 重启单个服务
docker compose restart flask-server
docker compose restart engine

# 查看资源占用
docker stats
```

### 3.3 重建容器

修改代码或 Dockerfile 后需要重建：

```bash
# 重建并重启某个服务
docker compose build flask-server
docker compose up -d flask-server

# 重建所有服务并重启
docker compose up -d --build
```

### 3.4 Engine 参数说明

当前 Engine 启动命令（在 `docker-compose.yml` 中）：

```yaml
command: ["--dogs", "10", "--ticks", "100000", "--interval", "1", "--seed", "99"]
```

| 参数 | 当前值 | 说明 |
|------|--------|------|
| `--dogs` | 10 | 模拟狗数量 |
| `--ticks` | 100000 | 总模拟步数（约 27 小时跑完） |
| `--interval` | 1 | 每秒生成一条数据 |
| `--seed` | 99 | 随机种子（固定则设备 ID 不变） |

> 如需长期运行，将 `--ticks 100000` 改为 `--ticks 0`（无限循环）。

---

## 4. Nginx 管理

```bash
# 检查配置语法
nginx -t

# 重载配置（不中断连接）
nginx -s reload
systemctl reload nginx

# 重启
systemctl restart nginx

# 查看状态
systemctl status nginx
```

### Nginx 配置要点

当前配置监听 80（HTTP）和 443（HTTPS），通过 Cloudflare 代理时：
- Cloudflare 到源站可以是 HTTP 或 HTTPS
- 源站 443 端口使用 Let's Encrypt 证书
- `/api/` 路径反向代理到 `127.0.0.1:5000`

---

## 5. SSL 证书

使用 Let's Encrypt，已设置自动续期。

```bash
# 手动续期测试
certbot renew --dry-run

# 查看证书信息
certbot certificates

# 证书文件位置
/etc/letsencrypt/live/pppetnode.com/
├── cert.pem
├── chain.pem
├── fullchain.pem    ← Nginx 引用的文件
└── privkey.pem      ← 私钥
```

Certbot 自动续期任务已配置在 systemd 定时器中，无需手动操作。证书到期 **2026-09-09**。

---

## 6. 日志查看

### 6.1 Docker 容器日志

```bash
# 查看 Flask 日志（最新 50 行 + 持续跟踪）
docker compose logs --tail=50 -f flask-server

# 查看 Engine 日志
docker compose logs --tail=50 -f engine

# 查看 MQ Worker 日志
docker compose logs --tail=50 -f mq-worker

# 查看 RabbitMQ 日志
docker compose logs --tail=20 rabbitmq
```

### 6.2 Nginx 日志

```bash
# 访问日志
tail -f /var/log/nginx/access.log

# 错误日志
tail -f /var/log/nginx/error.log
```

### 6.3 系统监控

```bash
# 磁盘使用
df -h

# 内存使用
free -h

# CPU 负载
top  # 按 q 退出
htop  # 如已安装

# Docker 资源占用
docker stats
```

---

## 7. 数据库操作

### 7.1 MongoDB

```bash
# 进入 MongoDB 容器
docker exec -it petnode-mongodb mongosh

# 常用操作
use petnode
db.received_records.count()          # 记录总数
db.received_records.find().limit(5)  # 查看最近 5 条
db.received_records.find({"device_id": "xxx"}).limit(10)
```

### 7.2 MySQL

```bash
# 进入 MySQL 容器
docker exec -it petnode-mysql mysql -u root -p
# 密码：见 .env 中 MYSQL_ROOT_PASSWORD

# 常用操作
USE petnode;
SHOW TABLES;
SELECT COUNT(*) FROM telemetry_record;
SELECT * FROM device LIMIT 10;
```

---

## 8. 备份与恢复

### 8.1 MongoDB 备份

```bash
# 备份（在服务器上执行）
docker exec petnode-mongodb mongodump \
  --db petnode \
  --out /tmp/mongo-backup-$(date +%Y%m%d)
docker cp petnode-mongodb:/tmp/mongo-backup-$(date +%Y%m%d) /opt/petnode/backups/

# 恢复
docker cp /opt/petnode/backups/mongo-backup-20260611 petnode-mongodb:/tmp/
docker exec petnode-mongodb mongorestore \
  --db petnode \
  /tmp/mongo-backup-20260611/petnode \
  --drop
```

### 8.2 MySQL 备份

```bash
# 备份
docker exec petnode-mysql mysqldump \
  -u root -p${MYSQL_ROOT_PASSWORD} \
  petnode > /opt/petnode/backups/mysql-backup-$(date +%Y%m%d).sql

# 恢复
cat /opt/petnode/backups/mysql-backup-20260611.sql | \
  docker exec -i petnode-mysql mysql -u root -p${MYSQL_ROOT_PASSWORD} petnode
```

### 8.3 数据卷备份

```bash
# 备份所有数据卷
docker run --rm \
  -v rabbitmq_data:/source/rabbitmq \
  -v mongo_data:/source/mongo \
  -v mysql_data:/source/mysql \
  -v /opt/petnode/backups:/backup \
  alpine tar czf /backup/volumes-$(date +%Y%m%d).tar.gz -C /source .
```

> 建议将备份文件定期下载到本地或上传到 OSS。

---

## 9. 更新部署

### 9.1 更新前端

```bash
# 1. 在本地打包前端
cd d:/bot/PetNode/PetNode.com\(final\)
tar czf frontend.tar.gz .

# 2. 上传到服务器
scp frontend.tar.gz root@47.108.239.80:/tmp/

# 3. 在服务器上解压
ssh root@47.108.239.80
tar xzf /tmp/frontend.tar.gz -C /var/www/pppetnode.com

# 4. 前端立即生效（纯静态文件）
```

### 9.2 更新后端代码

```bash
# 1. 在本地打包修改后的代码
cd d:/bot/PetNode/C_end_Simulator
tar czf backend-update.tar.gz flask_server/ engine/ docker-compose.yml

# 2. 上传到服务器
scp backend-update.tar.gz root@47.108.239.80:/opt/petnode/

# 3. 在服务器上解压并重建
ssh root@47.108.239.80
cd /opt/petnode/C_end_Simulator
tar xzf ../backend-update.tar.gz
docker compose build flask-server engine mq-worker
docker compose up -d
```

### 9.3 环境变量修改

修改 `/opt/petnode/C_end_Simulator/.env` 后需重建容器：

```bash
cd /opt/petnode/C_end_Simulator
# 编辑 .env
vim .env
# 重建受影响的服务
docker compose up -d
```

---

## 10. 系统监控

### 10.1 健康检查

```bash
# 本地检查
curl -s http://localhost:5000/api/health
# 预期: {"status":"healthy","timestamp":"...","total_received":...}

# 通过域名检查
curl -s https://pppetnode.com/api/health

# 前端可访问性
curl -so /dev/null -w "%{http_code}" https://pppetnode.com/
curl -so /dev/null -w "%{http_code}" https://pppetnode.com/admin.html
```

### 10.2 磁盘告警

当前磁盘使用：**40G 总量，约 27G 可用（29% 已用）**。  
建议在磁盘使用超过 80% 时清理旧数据或扩容。

### 10.3 内存告警

当前内存：**3.5G 总量，约 1.8G 可用**。  
如果容器频繁 OOM（Out of Memory），考虑升级实例规格。

---

## 11. 常见问题排查

### 11.1 容器无法启动

```bash
# 查看具体错误
docker compose logs flask-server

# 检查 Docker 是否运行
systemctl status docker

# 重建并重试
docker compose down
docker compose up -d
```

### 11.2 Nginx 502 Bad Gateway

表示 Nginx 无法连接到 Flask 后端：

```bash
# 检查 Flask 是否运行
docker compose ps flask-server
docker compose logs --tail=20 flask-server

# 检查端口监听
curl http://localhost:5000/api/health
```

### 11.3 Engine 报错

```bash
docker compose logs --tail=50 engine
```

常见原因：
- Flask 未就绪（Engine 有 `depends_on` 依赖）
- RabbitMQ 连接失败
- API_KEY/HMAC_KEY 不匹配

### 11.4 磁盘空间不足

```bash
# 清理 Docker 未使用的资源
docker system prune -f

# 清理已停止的容器和未使用的镜像
docker container prune
docker image prune

# 查看各数据卷大小
docker system df
```

### 11.5 修改后不生效

修改了代码或配置后，需要重建镜像：

```bash
docker compose build flask-server
docker compose up -d flask-server
```

静态前端文件修改后**无需**重启，浏览器刷新即可。

### 11.6 SSL 证书问题

```bash
# 检查证书状态
certbot certificates

# 强制续期
certbot renew --force-renewal

# 检查自动续期任务
systemctl status certbot.timer
```

---

## 12. 密钥一览

> 以下为部署时自动生成的密码，请妥善保管。

| 密钥项 | 值 | 位置 |
|--------|-----|------|
| 服务器 SSH 密码 | `1145141919810#Homo` | SSH 登录 |
| 管理员用户名 | `admin` | 管理大屏登录 |
| 管理员密码 | `d6ead38ffcfd4c83` | 管理大屏登录 |
| MySQL Root 密码 | 见 `/opt/petnode/C_end_Simulator/.env` | `.env` 文件 |
| MySQL App 密码 | 见 `.env` | `.env` 文件 |
| JWT 密钥 | 见 `.env` | `.env` 文件 |
| API 密钥 | 见 `.env` | `.env` 文件 |
| HMAC 密钥 | 见 `.env` | `.env` 文件 |

> **安全提醒**：密钥以明文存储在 `.env` 文件中。如需共享，使用安全渠道传输。定期更换生产密钥。

---

## 附录: 速查命令表

| 操作 | 命令 |
|------|------|
| SSH 登录 | `ssh root@47.108.239.80` |
| 查看容器状态 | `docker compose ps` |
| 查看 Flask 日志 | `docker compose logs --tail=50 -f flask-server` |
| 重启服务 | `docker compose restart flask-server` |
| 重建服务 | `docker compose up -d --build flask-server` |
| 停止所有 | `docker compose down` |
| 健康检查 | `curl https://pppetnode.com/api/health` |
| 重载 Nginx | `systemctl reload nginx` |
| 证书信息 | `certbot certificates` |
| 磁盘检查 | `df -h` |
| MongoDB 查询 | `docker exec -it petnode-mongodb mongosh` |
| MySQL 查询 | `docker exec -it petnode-mysql mysql -u root -p` |

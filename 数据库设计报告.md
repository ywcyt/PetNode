# PetNode 数据库设计报告

> **项目名称**：PetNode — 宠物智能项圈数据平台  
> **报告日期**：2026-06-21  
> **分支**：last

---

## 目录

1. [总体架构](#1-总体架构)
2. [数据库选型](#2-数据库选型)
3. [MongoDB 详细设计](#3-mongodb-详细设计)
4. [MySQL 详细设计](#4-mysql-详细设计)
5. [存储层设计](#5-存储层设计)
6. [数据流与写入策略](#6-数据流与写入策略)
7. [数据模型](#7-数据模型)
8. [索引设计](#8-索引设计)
9. [异常检测机制](#9-异常检测机制)
10. [安全与认证](#10-安全与认证)
11. [关键设计决策](#11-关键设计决策)
12. [文件索引](#12-文件索引)

---

## 1. 总体架构

PetNode 采用 **双数据库异构存储** 架构，将实时业务查询与分析存储分离：

```
                        ┌─────────────────┐
                        │   SmartCollar    │
                        │   (智能项圈模拟器)  │
                        └────────┬────────┘
                                 │
                                 ▼
                        ┌─────────────────┐
                        │  Flask API / MQ  │
                        │  (HMAC 签名校验)   │
                        └────────┬────────┘
                                 │
              ┌──────────────────┼──────────────────┐
              ▼                  ▼                  ▼
     ┌────────────┐    ┌────────────┐    ┌────────────┐
     │ FileStorage│    │MongoStorage│    │MySQLStorage│
     │  (JSONL)   │    │   (主库)    │    │  (分析库)   │
     └────────────┘    └─────┬──────┘    └────────────┘
                             │
              ┌──────────────┼──────────────┐
              ▼              ▼              ▼
     ┌──────────┐  ┌──────────┐  ┌──────────────────┐
     │  用户认证  │  │  宠物遥测  │  │  家庭共享/管理后台 │
     │ (微信绑定) │  │ (数据查询)  │  │  (权限与群组)      │
     └──────────┘  └──────────┘  └──────────────────┘
```

### 技术栈

| 组件 | 技术 | 版本 |
|---|---|---|
| 文档数据库 | MongoDB | 7.x |
| 关系数据库 | MySQL | 8.x |
| 消息队列 | RabbitMQ | 3.x (management) |
| MongoDB 驱动 | pymongo | ≥4.6 |
| MySQL 驱动 | PyMySQL | ≥1.1 |
| 消息队列客户端 | pika | ≥1.3 |

---

## 2. 数据库选型

| 维度 | MongoDB | MySQL |
|---|---|---|
| **角色** | 主运行库（OLTP） | 辅助分析库（OLAP） |
| **数据类型** | 半结构化文档 | 规范化关系表 |
| **查询场景** | REST API 实时查询 | 统计分析、异常检测 |
| **写入优先级** | 必须成功 | 允许降级 |
| **ORM** | 无（直接 pymongo） | 无（直接 PyMySQL） |

**选型理由**：
- **MongoDB**：智能项圈上报的遥测数据字段灵活多变（不同事件携带不同字段），文档模型天然适配，无需频繁变更表结构。
- **MySQL**：用于分析型查询（聚合、关联、异常统计），结构化数据更适合 SQL 分析语句。

---

## 3. MongoDB 详细设计

### 3.1 集合清单

共计 **10 个集合**：

#### `received_records`（核心数据集合）

存储全部原始遥测数据，所有数据查询入口。

| 字段 | 类型 | 说明 |
|---|---|---|
| `_id` | ObjectId | 自动主键 |
| `device_id` | String | 设备序列号 |
| `user_id` | String | 关联用户 ID |
| `timestamp` | DateTime | 数据时间戳 |
| `behavior` | String | 行为状态 |
| `heart_rate` | Float | 心率 (bpm) |
| `resp_rate` | Float | 呼吸率 (次/分) |
| `temperature` | Float | 体温 (°C) |
| `steps` | Int | 步数 |
| `battery` | Float | 电量 (%) |
| `gps_lat` | Float | GPS 纬度 |
| `gps_lng` | Float | GPS 经度 |
| `event` | String | 事件标识 |
| `event_phase` | String | 事件阶段 (onset/peak/recovery) |
| `ingested_at` | DateTime | 入库时间戳 |

#### `users`（用户集合）

| 字段 | 类型 | 说明 |
|---|---|---|
| `_id` | ObjectId | 自动主键 |
| `user_id` | String | 业务用户 ID |
| `user_hash` | String | 用户身份哈希 |
| `nick_name` | String | 昵称 |
| `avatar_url` | String | 头像 URL |

#### `wechat_bindings`（微信绑定）

| 字段 | 类型 | 说明 |
|---|---|---|
| `user_id` | String | 系统用户 ID |
| `openid` | String | 微信 OpenID |
| `unionid` | String | 微信 UnionID（可空） |

#### `user_pets`（用户-宠物绑定）

| 字段 | 类型 | 说明 |
|---|---|---|
| `user_id` | String | 用户 ID |
| `device_id` | String | 设备 ID |
| `pet_id` | String | 宠物 ID |
| `role` | String | 角色 (owner/viewer) |

#### `pets`（宠物档案）

| 字段 | 类型 | 说明 |
|---|---|---|
| `pet_id` | String | 宠物 ID |
| `pet_name` | String | 宠物名称 |
| `breed` | String | 品种 |
| `age` | Float | 年龄 |
| `photo_url` | String | 照片 URL |

#### `families`（家庭群组）

| 字段 | 类型 | 说明 |
|---|---|---|
| `family_id` | String | 家庭 ID |
| `family_name` | String | 家庭名称 |
| `owner_user_id` | String | 群主用户 ID |

#### `family_members`（家庭成员）

| 字段 | 类型 | 说明 |
|---|---|---|
| `family_id` | String | 家庭 ID |
| `user_id` | String | 成员用户 ID |
| `role` | String | 角色 (admin/member) |

#### `family_invites`（家庭邀请）

| 字段 | 类型 | 说明 |
|---|---|---|
| `family_id` | String | 家庭 ID |
| `inviter_user_id` | String | 邀请人 |
| `invitee_user_id` | String | 被邀请人 |
| `invite_token` | String | 邀请令牌 |
| `status` | String | 状态 (pending/accepted/rejected) |
| `expires_at` | DateTime | 过期时间 |

#### `pet_event_reads`（事件已读追踪）

| 字段 | 类型 | 说明 |
|---|---|---|
| `user_id` | String | 用户 ID |
| `pet_id` | String | 宠物 ID |
| `event_id` | String | 事件 ID |
| `read_at` | DateTime | 已读时间 |

#### `chat_questions`（AI 问答历史）

| 字段 | 类型 | 说明 |
|---|---|---|
| `user_id` | String | 用户 ID |
| `question` | String | 用户问题 |
| `answer` | String | AI 回答 |
| `created_at` | DateTime | 提问时间 |

### 3.2 ER 关系（文档引用）

```
users ────< wechat_bindings          (1:N, 用户可有多个微信绑定)
users ────< user_pets >──── pets     (M:N, 用户与宠物多对多绑定)
users ────< family_members >─ families (M:N, 用户与家庭多对多)
families ──< family_invites          (1:N, 家庭发出多个邀请)
pets ────< received_records         (1:N, 宠物产生多条遥测记录)
users ────< pet_event_reads >─ pets  (M:N, 用户-宠物-事件已读)
users ────< chat_questions           (1:N, 用户提问记录)
```

---

## 4. MySQL 详细设计

### 4.1 表结构

共计 **8 张表**：

#### `user`（用户表）

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `user_id` | BIGINT | PK, AUTO_INCREMENT | 用户主键 |
| `username` | VARCHAR(64) | UNIQUE, NOT NULL | 用户名 |
| `password_hash` | VARCHAR(255) | NOT NULL | 密码哈希 |
| `phone` | VARCHAR(20) | — | 手机号 |
| `nick_name` | VARCHAR(64) | — | 昵称 |
| `created_at` | DATETIME | — | 创建时间 |
| `updated_at` | DATETIME | — | 更新时间 |

#### `device`（设备表）

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `device_id` | BIGINT | PK | 设备主键（由 SN 哈希生成） |
| `user_id` | BIGINT | FK → user | 所属用户 |
| `device_sn` | VARCHAR(128) | UNIQUE, NOT NULL | 设备序列号 |
| `device_name` | VARCHAR(64) | — | 设备名称 |
| `pet_name` | VARCHAR(64) | — | 宠物名 |
| `is_online` | TINYINT(1) | DEFAULT 0 | 在线状态 |
| `created_at` | DATETIME | — | 创建时间 |
| `updated_at` | DATETIME | — | 更新时间 |

#### `trait_type`（特征类型字典）

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `trait_type_id` | INT | PK, AUTO_INCREMENT | 特征类型主键 |
| `trait_code` | VARCHAR(32) | UNIQUE, NOT NULL | 特征编码 |
| `trait_name` | VARCHAR(64) | NOT NULL | 特征名称 |
| `trait_unit` | VARCHAR(16) | — | 单位 |

**预置数据**：

| trait_code | trait_name | trait_unit |
|---|---|---|
| `heart_rate` | 心率 | bpm |
| `resp_rate` | 呼吸率 | 次/分 |
| `temperature` | 体温 | °C |
| `steps` | 步数 | 步 |
| `battery` | 电量 | % |
| `gps_lat` | GPS纬度 | 度 |
| `gps_lng` | GPS经度 | 度 |
| `behavior` | 行为 | — |

#### `device_trait`（设备-特征关联）

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `device_id` | BIGINT | FK → device | 设备 ID |
| `trait_type_id` | INT | FK → trait_type | 特征类型 ID |
| `is_enabled` | TINYINT(1) | DEFAULT 1 | 是否启用 |

> 联合主键：`(device_id, trait_type_id)`

#### `event_type`（事件类型字典）

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `event_type_id` | INT | PK, AUTO_INCREMENT | 事件类型主键 |
| `event_code` | VARCHAR(32) | UNIQUE, NOT NULL | 事件编码 |
| `event_name` | VARCHAR(64) | NOT NULL | 事件名称 |
| `event_level` | INT | — | 事件级别 |

**预置数据**：

| event_code | event_name | event_level |
|---|---|---|
| `fever` | 发烧 | — |
| `injury` | 受伤 | — |

#### `event_instance`（事件实例）

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `event_instance_id` | INT | PK, AUTO_INCREMENT | 事件实例主键 |
| `device_id` | BIGINT | FK → device | 设备 ID |
| `event_type_id` | INT | FK → event_type | 事件类型 |
| `status` | ENUM('active','closed') | — | 事件状态 |
| `event_content` | TEXT | — | 事件内容 |
| `start_time` | DATETIME | — | 开始时间 |
| `end_time` | DATETIME | — | 结束时间 |

#### `telemetry_record`（遥测记录表 —— 核心）

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `record_id` | BIGINT | PK, AUTO_INCREMENT | 记录主键 |
| `user_id` | BIGINT | FK → user | 用户 ID |
| `device_id` | BIGINT | FK → device | 设备 ID |
| `event_instance_id` | INT | FK → event_instance | 关联事件实例 |
| `trait_type_id` | INT | FK → trait_type | 特征类型 |
| `trait_value` | DOUBLE | — | 特征值 |
| `timestamp` | DATETIME | — | 时间戳 |

#### `anomaly_record`（异常记录表）

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `anomaly_id` | BIGINT | PK, AUTO_INCREMENT | 异常主键 |
| `user_id` | BIGINT | FK → user | 用户 ID |
| `device_id` | BIGINT | FK → device | 设备 ID |
| `event_instance_id` | INT | FK → event_instance | 关联事件实例 |
| `anomaly_code` | VARCHAR(64) | — | 异常编码 |
| `anomaly_detail` | JSON | — | 异常详情 (JSON) |
| `created_at` | DATETIME | — | 创建时间 |
| `resolved_at` | DATETIME | — | 解决时间 |

### 4.2 ER 图

```
user ──< device ──< device_trait >── trait_type
  │        │
  │        ├──< event_instance >── event_type
  │        │        │
  │        │        ├──< anomaly_record
  │        │        │
  │        ├────────│────< telemetry_record >── trait_type
  │                 │
  └─────────────────┘
```

---

## 5. 存储层设计

### 5.1 策略模式实现

```
BaseStorage (抽象基类)
  │
  ├── FileStorage     → received.jsonl    (开发调试用)
  ├── MongoStorage    → MongoDB           (生产主库)
  └── MySQLStorage    → MySQL             (分析仓库)
```

**接口定义**：`save(record)` / `close()`

### 5.2 各后端特点

| 后端 | 适用场景 | 特点 |
|---|---|---|
| **FileStorage** | 本地开发、单机测试 | 线程安全，JSONL 追加写入 |
| **MongoStorage** | 生产 OLTP | 文档灵活，高写入吞吐 |
| **MySQLStorage** | 数据分析 | 规范化表结构，适配 SQL 聚合查询 |

### 5.3 延迟初始化代理

使用 `_LazyProxy` 模式初始化存储后端，避免循环依赖，按需创建实例。

```python
mongo_storage = _LazyProxy(lambda: MongoStorage(...))
mysql_storage = _LazyProxy(lambda: MySQLStorage(...))
```

---

## 6. 数据流与写入策略

### 6.1 写入流程

```
record = {device_id, timestamp, heart_rate, ...}
            │
            ▼
   ┌─ 1. HMAC 签名校验 ─┐
   └────────────────────┘
            │
            ▼
   ┌─ 2. MongoStorage.save(record)  ← 必须成功
   │      写入 received_records
   └────────────────────┘
            │
            ▼
   ┌─ 3. MySQLStorage.save(record)  ← 允许降级
   │      a. 设备ID映射 (SHA-256)
   │      b. 事件生命周期管理
   │      c. 特征值归一化 → telemetry_record
   │      d. 异常检测 → anomaly_record
   └────────────────────┘
```

### 6.2 双通道写入

| 通道 | 入口 | 用途 |
|---|---|---|
| **HTTP API** | `POST /api/data` | 同步写入 |
| **消息队列** | RabbitMQ Consumer | 异步批量写入 |

两通道使用完全相同的 `_persist_record()` 逻辑，保证数据一致性。

### 6.3 降级策略

- MongoDB 写入失败 → **整体失败**，返回错误
- MySQL 写入失败 → **仅记日志**，不阻塞上游
- 环境变量 `CLEAN_ON_STARTUP` 控制是否在启动时清空历史数据

---

## 7. 数据模型

### 7.1 遥测数据记录（12 字段标准格式）

由 `SmartCollar.generate_one_record()` 生成：

```json
{
  "device_id": "DEVICE_SN_001",
  "timestamp": "2026-06-21T12:00:00Z",
  "behavior": "running",
  "heart_rate": 120.5,
  "resp_rate": 30.2,
  "temperature": 38.7,
  "steps": 842,
  "battery": 85.0,
  "gps_lat": 31.2304,
  "gps_lng": 121.4737,
  "event": "fever",
  "event_phase": "peak"
}
```

### 7.2 生理基线（DogProfile 数据类）

| 属性 | 说明 |
|---|---|
| `breed_size` | 品种体型 (small/medium/large) |
| `age_stage` | 年龄阶段 (puppy/adult/senior) |
| `traits` | 健康特征列表 (CardiacRisk/RespiratoryRisk/OrthoRisk) |
| `home_lat/home_lng` | GPS 家庭位置 |

### 7.3 健康特征影响

| 特征类 | 对数据的影响 |
|---|---|
| `CardiacRisk` | 心率基线偏移、行为概率调整 |
| `RespiratoryRisk` | 呼吸率基线偏移 |
| `OrthoRisk` | 步数调整、GPS 移动模式变化 |

### 7.4 事件生命周期

```
onset (起始) → peak (峰值) → recovery (恢复)
```

| 事件 | 起始特征 | 峰值特征 | 恢复特征 |
|---|---|---|---|
| `fever` | 体温上升 | 体温最高 | 体温回落 |
| `injury` | 步数骤降 | 行为异常 | 逐步恢复 |

---

## 8. 索引设计

### 8.1 MongoDB 索引

| 集合 | 索引 | 类型 | 说明 |
|---|---|---|---|
| `received_records` | `(device_id, timestamp DESC)` | 复合 | 按设备查询遥测数据 |
| `received_records` | `(user_id, timestamp)` | 复合 | 按用户查询遥测数据 |
| `wechat_bindings` | `openid` | 唯一 | 微信登录查找 |
| `wechat_bindings` | `unionid` | 唯一稀疏 | UnionID 查找 |
| `wechat_bindings` | `user_id` | 普通 | 反向查询 |
| `user_pets` | `(user_id, device_id)` | 唯一 | 用户-设备绑定 |
| `user_pets` | `device_id` | 唯一 | 设备独占绑定 |
| `pets` | `pet_id` | 唯一 | 宠物查找 |
| `families` | `family_id` | 唯一 | 家庭查找 |
| `families` | `owner_user_id` | 唯一 | 群主查找 |
| `family_members` | `(family_id, user_id)` | 唯一 | 成员关系 |
| `family_members` | `user_id` | 普通 | 反向查询 |
| `family_invites` | `invite_token` | 唯一 | 邀请令牌查找 |
| `family_invites` | `expires_at` | TTL | 自动清理过期邀请 |
| `pet_event_reads` | `(user_id, pet_id, event_id)` | 唯一 | 已读状态 |

### 8.2 MySQL 索引

MySQL 表索引由主键（PK）和外键（FK）自动覆盖，`mysql_storage.py` 中未创建额外二级索引。

---

## 9. 异常检测机制

### 9.1 内置规则

检测位于 `MySQLStorage._detect_anomaly()` 方法中，基于硬编码阈值：

| 指标 | 低阈值 | 高阈值 | 异常编码 |
|---|---|---|---|
| 心率（heart_rate） | < 30 bpm | > 180 bpm | `BRADYCARDIA` / `TACHYCARDIA` |
| 呼吸率（resp_rate） | < 8 次/分 | > 60 次/分 | `BRADYPNEA` / `TACHYPNEA` |
| 体温（temperature） | < 36°C | > 40.5°C | `HYPOTHERMIA` / `HYPERTHERMIA` |

### 9.2 触发流程

```
telemetry_record 写入
        │
        ▼
  阈值比较 (_detect_anomaly)
        │
        ▼
  超出阈值 → anomaly_record 写入 (anomaly_detail 为 JSON)
  ├── anomaly_code: "TACHYCARDIA"
  ├── trait_type: "heart_rate"
  ├── current_value: 185.0
  └── threshold_high: 180.0
```

---

## 10. 安全与认证

### 10.1 认证体系

| 组件 | 方案 | 说明 |
|---|---|---|
| API 认证 | HMAC 签名 | 设备上报数据签名校验 |
| API 密钥 | API_KEY 环境变量 | 服务间调用认证 |
| 用户认证 | JWT Token | 双 Token 机制 |
| `access_token` | 7 天有效期 | 常规 API 鉴权 |
| `wx_identity_token` | 10 分钟有效期 | 微信扫码绑定场景 |
| 微信认证 | OpenID / UnionID | 微信小程序登录 |

### 10.2 设备 ID 哈希

```
device_sn (字符串) ── SHA-256 ──> 32 字节哈希 ──> 取前 8 字节 ──> BIGINT
```

- **确定性**：同一 SN 始终生成相同 ID，保证幂等
- **安全性**：不可逆，不暴露原始 SN
- **冲突概率**：64 位空间，极低

### 10.3 权限控制

| 接口类 | 权限检查 |
|---|---|
| 设备绑定 | `assert_user_owns_pet()` |
| 数据查询 | `assert_user_can_access_pet()` — 所有者 + 家庭成员 |
| 家庭管理 | `family_members` 角色检查 |
| 管理后台 | 管理员专用路由 |

---

## 11. 关键设计决策

| # | 决策 | 理由 |
|---|---|---|
| 1 | **无 ORM 框架** | 项目规模适中，原始驱动避免抽象开销，代码透明 |
| 2 | **无 SQL 迁移文件** | Schema 由 `_ensure_schema()` 自动创建，简化部署 |
| 3 | **MySQL 优雅降级** | MongoDB 为主库，MySQL 故障不影响核心数据接收 |
| 4 | **双 Token 设计** | 长 Token 用于日常鉴权，短 Token 确保扫码绑定安全窗口 |
| 5 | **SHA-256 设备 ID** | 确定性映射保证幂等，同时隐藏原始 SN |
| 6 | **消息队列解耦** | RabbitMQ 异步写入削峰填谷，提高系统吞吐 |
| 7 | **家庭共享模型** | 三集合（families/members/invites）实现完整的群组管理 |
| 8 | **策略模式存储** | 三后端可插拔，环境变量切换，开发/生产灵活配置 |

---

## 12. 文件索引

### 数据库核心文件

| 文件 | 说明 |
|---|---|
| [C_end_Simulator/flask_server/db.py](C_end_Simulator/flask_server/db.py) | MongoDB 连接管理、索引创建 |
| [C_end_Simulator/flask_server/storage/base_storage.py](C_end_Simulator/flask_server/storage/base_storage.py) | 存储层抽象基类 |
| [C_end_Simulator/flask_server/storage/mongo_storage.py](C_end_Simulator/flask_server/storage/mongo_storage.py) | MongoDB 存储实现 |
| [C_end_Simulator/flask_server/storage/mysql_storage.py](C_end_Simulator/flask_server/storage/mysql_storage.py) | MySQL 存储实现（含完整建表逻辑） |
| [C_end_Simulator/flask_server/storage/file_storage.py](C_end_Simulator/flask_server/storage/file_storage.py) | 文件存储实现 |
| [C_end_Simulator/flask_server/app.py](C_end_Simulator/flask_server/app.py) | Flask 主应用（数据写入编排） |
| [C_end_Simulator/flask_server/mq_worker.py](C_end_Simulator/flask_server/mq_worker.py) | RabbitMQ 消费者（异步写入） |
| [C_end_Simulator/docker-compose.yml](C_end_Simulator/docker-compose.yml) | 数据库容器编排 |
| [C_end_Simulator/.env](C_end_Simulator/.env) | 数据库账号密码配置 |

### 业务逻辑层

| 文件 | 说明 |
|---|---|
| [C_end_Simulator/flask_server/blueprints/wechat.py](C_end_Simulator/flask_server/blueprints/wechat.py) | 微信认证 API — 操作 `wechat_bindings` |
| [C_end_Simulator/flask_server/blueprints/users.py](C_end_Simulator/flask_server/blueprints/users.py) | 用户 API — 操作 `users`, `user_pets` |
| [C_end_Simulator/flask_server/blueprints/pets.py](C_end_Simulator/flask_server/blueprints/pets.py) | 宠物遥测 API — 操作 `received_records`, `pets` |
| [C_end_Simulator/flask_server/blueprints/devices.py](C_end_Simulator/flask_server/blueprints/devices.py) | 设备绑定 API — 操作 `user_pets`, `pets` |
| [C_end_Simulator/flask_server/blueprints/family.py](C_end_Simulator/flask_server/blueprints/family.py) | 家庭 API — 操作 `families`, `family_members`, `family_invites` |
| [C_end_Simulator/flask_server/blueprints/admin.py](C_end_Simulator/flask_server/blueprints/admin.py) | 管理后台 API |
| [C_end_Simulator/flask_server/services/binding.py](C_end_Simulator/flask_server/services/binding.py) | 绑定服务（用户-微信、用户-设备） |
| [C_end_Simulator/flask_server/services/telemetry.py](C_end_Simulator/flask_server/services/telemetry.py) | 遥测查询服务 |
| [C_end_Simulator/flask_server/services/family.py](C_end_Simulator/flask_server/services/family.py) | 家庭管理服务 |

### 数据生成层

| 文件 | 说明 |
|---|---|
| [C_end_Simulator/engine/models/smart_collar.py](C_end_Simulator/engine/models/smart_collar.py) | 智能项圈数据生成器 |
| [C_end_Simulator/engine/models/dog_profile.py](C_end_Simulator/engine/models/dog_profile.py) | 犬只生理档案模型 |
| [C_end_Simulator/engine/traits/](C_end_Simulator/engine/traits/) | 健康特征（心脏/呼吸/骨科） |
| [C_end_Simulator/engine/events/](C_end_Simulator/engine/events/) | 事件模拟（发烧/受伤） |

### 文档

| 文件 | 说明 |
|---|---|
| [C_end_Simulator/MYSQL_SETUP.md](C_end_Simulator/MYSQL_SETUP.md) | MySQL 部署与使用指南 |
| [C_end_Simulator/API_Documentation.md](C_end_Simulator/API_Documentation.md) | API 文档（含数据模型章节） |
| [README.md](README.md) | 项目总览（含数据库结构章节） |

---

---

## 补充：企业级 Web 端大屏实现细节

前端工程基于 **Vue 3 + Element Plus + ECharts 5.5** 构建，通过 CDN importmap 方式加载 ESM 模块，无需前端构建工具即可运行。大屏页面 `admin.html` 采用 60%/40% 左右分栏布局：左侧上方为重庆区域地图，下方嵌入 AI 智能简报窗口；右侧为 7 个可折叠维度面板（犬型分布、心率、行为状态、呼吸频率、体温、步频、异常事件），每个面板支持 Element Plus 单选按钮切换实时模式/过去24小时/过去7天三种时间粒度，实时模式渲染 ECharts 环形饼图，历史模式渲染按重庆四大区域（渝北、渝西、渝南、渝东）分组的堆叠柱状图。地图交互模块未经由第三方地图 SDK，而是以重庆静态底图 `chongqing.png` 作为 CSS 背景，叠加 ECharts `cartesian2d` 坐标系实现完全自制的 GIS 可视化层。地图边界硬编码为经度 106.38–106.60、纬度 29.48–29.64，覆盖重庆主城八区（渝北、江北、沙坪坝、渝中、南岸、九龙坡、大渡口、巴南），设备 GPS 坐标通过 `Math.hypot()` 距离计算自动分配到最近行政区。高频热力点渲染通过 ECharts 多层系列叠加实现：底层为异常点周围随机散布的 8 个半透明圆形（模拟热雾扩散效果），中层为 `effectScatter` 涟漪动画标记异常设备，上层为按心率缩放的设备散点气泡（`symbolSize` 函数映射 8–18px），悬停时弹出富文本标签显示设备 ID 和当前维度指标值（带 ok/warn/danger 颜色编码）。区域内异常设备数超过 2 台的行政区还会渲染红色警告三角形标记和红色半透明警示圈。地图支持拖动校准模式（通过鼠标拖拽调整背景偏移量与缩放比，设置持久化至 localStorage），切换维度面板时触发 CSS 幕帘动画过渡。内置 AI 窗口调用后端 `POST /api/chat` 接口，前端将用户输入文本 POST 至 Flask 服务，后端 `_build_data_context()` 从 MongoDB 聚合最近 200 条遥测记录，按设备计算平均心率、体温、呼吸率并标记异常标签（发热/心率过高/心率过低），生成文本格式的"大盘快照"；随后调用 **DeepSeek API**（`deepseek-chat` 模型），系统提示将其塑造为 PetNode 健康分析师角色，要求根据实时遥测数据分析心率、体温、呼吸率、行为模式、异常事件和设备电量，以中文 Markdown 格式返回。响应以消息气泡形式渲染（系统消息靠左、用户消息靠右），支持 Enter 键快捷发送，自动滚动至最新消息。数据刷新采用 3 秒轮询机制（`setInterval` 调用 `refreshAll`），并行请求三个端点：`/api/health`（连接检查）、`/api/profile?source=mongo`（设备档案含宠物名称与最新 GPS）、`/api/records?source=mongo&limit=200`（最新遥测记录）、`/api/records?source=mysql&kind=anomalies&limit=100`（MySQL 异常数据）。MySQL 异常记录的 `device_id`（BIGINT）通过 profile 接口返回的映射表还原为设备 SN 字符串，然后解析 `anomaly_detail` JSON 字段中的 trait 关键字归类为心脏异常/呼吸异常/骨骼异常三类，合并到实时设备列表中联动更新地图标记和维度面板告警。大屏顶部青色标题栏左侧展示 PETNODE 品牌标识与"管理大屏·态势感知"副标题，右侧为 LIVE 实时连接指示灯（CSS 脉冲动画，断连时切换为灰色离线模式）、每秒刷新的数字时钟和退出按钮。设备详情面板支持下拉选择单台设备，展示 8 指标网格（心率/体温/呼吸/步数/电量/行为/纬度/经度）及多系列 ECharts 折线图（心率、体温使用左 Y 轴，呼吸使用右 Y 轴，`yAxisIndex: 1`），平滑曲线无数据点标记。网络层 `api.js` 封装统一 fetch 请求函数，支持自动检测开发/生产环境 API 基地址、JWT Bearer Token 注入、401 过期事件通知，前端管理员入口通过硬编码凭据 `Test_Endmin` / `Endfiled_Best` 校验后存储 token 并跳转大屏页面，生产环境由 Nginx 反向代理将 `/api/` 转发至 Flask 5000 端口，域名 `pppetnode.com` 服务器 IP `47.108.239.80`。

*本报告基于 `last` 分支代码自动生成，数据截止 2026-06-21。*
